'''
Algoritimo Insertion Sort - Ordenação por Inserção
- Compara elemento por elemento, posicionando o menor na posição correta(inserção)

Complexidade: 0(n^2) - pior caso | o(n) - melhor caso

'''


def insertion_sort(lista):
    '''
    Algoritmo de ordenação por inserção( Insertion Sort)

    '''
    n = len(lista)

    for i in range(1 , n): 
        pivo = lista [i]
        j=i-1
        while j>=0 and pivo<lista[j]:
            lista [j+1]  = lista[j]
            j-=1
        lista[j+1] = pivo

lista = [12, 11, 13, 5 ,6]
print(f'Lista Original: {lista}')
insertion_sort(lista)
print(f'Lista Ordenada: {lista}')
